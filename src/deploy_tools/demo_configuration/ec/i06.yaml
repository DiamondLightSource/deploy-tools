# yaml-language-server: $schema=/workspaces/deploy-tools/src/deploy_tools/models/schemas/release.json

module:
  name: ec
  version: i06
  description: ec command line tool configured for i06
  allow_updates: true

  dependencies:
    - name: edge-containers-cli
    - name: plandev
    - name: k8s-i06
      version: local

  env_vars:
    - name: EC_TARGET
      value: i06-beamline/i06
    - name: EC_K8S_NAMESPACE
      value: i06-beamline
    - name: EC_SERVICES_REPO
      value: https://gitlab.diamond.ac.uk/controls/containers/beamline/i06-services.git
    - name: EC_BEAMLINE
      value: i06

  applications: []

  load_script:
    - system kubectl config set-context --current --namespace=i06-beamline
