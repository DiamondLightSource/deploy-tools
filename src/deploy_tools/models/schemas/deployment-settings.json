{
  "$defs": {
    "DefaultVersionsByName": {
      "additionalProperties": {
        "type": "string"
      },
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "All global configuration settings for the Deployment.",
  "properties": {
    "default_versions": {
      "$ref": "#/$defs/DefaultVersionsByName",
      "default": {},
      "description": "Mapping of <module name>: <version> to use as default when no version is specified"
    }
  },
  "title": "DeploymentSettings",
  "type": "object"
}
