# yaml-language-server: $schema=../../models/schemas/module.json

name: fancy-tools
version: "2.1"

dependencies:
  - name: dls-pmac-control
    version: "2.4"
  - name: something-else
    version: "3.6"

environment_vars:
  - name: EPICS_BASE
    value: /path/to/epics
