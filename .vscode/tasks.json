// See https://go.microsoft.com/fwlink/?LinkId=733558
// for the documentation about the tasks.json format
{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Tests, lint and docs",
            "command": "tox -p",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "problemMatcher": [],
        },
        {
            "label": "Update schema",
            "type": "shell",
            "command": "generate-schema ${workspaceFolder}/src/deploytools/models/schemas/",
            "problemMatcher": []
        }
    ]
}